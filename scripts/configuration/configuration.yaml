homeassistant:
  # Name of the location where Home Assistant is running
  name: Home
  # Location required to calculate the time the sun rises and sets
  latitude: 0
  longitude: 0
  # Impacts weather/sunrise data (altitude above sea level in meters)
  elevation: 0
  # metric for Metric, imperial for Imperial
  unit_system: metric
  # Pick yours from here: http://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  time_zone: UTC
  # Customization file
  customize: !include customize.yaml

# Show links to resources in log and frontend
introduction:

# Enables the frontend
frontend:

# Enables configuration UI
config:

# Uncomment this if you are using SSL/TLS, running in Docker container, etc.
# http:
#   base_url: example.duckdns.org:8123

# Checks for available updates
# Note: This component will send some information about your system to
# the developers to assist with development of Home Assistant.
# For more information, please see:
# https://home-assistant.io/blog/2016/10/25/explaining-the-updater/
updater:
  # Optional, allows Home Assistant developers to focus on popular components.
  # include_used_components: true

# Discover some devices automatically
discovery:

# Allows you to issue voice commands from the frontend in enabled browsers
conversation:

# Enables support for tracking state changes over time
history:

# View all events in a logbook
logbook:

# Enables a map showing the location of tracked devices
map:

# Track the sun
sun:

# Weather prediction
sensor:
  - platform: yr

# Text to speech
tts:
  - platform: google

# Cloud
cloud:

group: !include groups.yaml
automation: !include automations.yaml
script: !include scripts.yaml

mqtt:
  broker: localhost
  username: yurii
  password: 03213

switch:
  - platform: mqtt
    name: "Bedroom Switch1"
    state_topic: "home/bedroom/switch1/status"
    command_topic: "home/bedroom/switch1"
    payload_on: "ON"
    payload_off: "OFF"
    state_on: "ON"
    state_off: "OFF"

  - platform: mqtt
    name: "Bedroom Switch2"
    state_topic: "home/bedroom/switch2/status"
    command_topic: "home/bedroom/switch2"
    payload_on: "ON"
    payload_off: "OFF"
    state_on: "ON"
    state_off: "OFF"

  - platform: mqtt
    name: "Bedroom Switch3"
    state_topic: "home/bedroom/switch3/status"
    command_topic: "home/bedroom/switch3"
    payload_on: "ON"
    payload_off: "OFF"
    state_on: "ON"
    state_off: "OFF"

  - platform: mqtt
    name: "Bedroom Switch4"
    state_topic: "home/bedroom/switch4/status"
    command_topic: "home/bedroom/switch4"
    payload_on: "ON"
    payload_off: "OFF"
    state_on: "ON"
    state_off: "OFF"

  - platform: mqtt
    name: "Bedroom Switch5"
    state_topic: "home/bedroom/switch5/status"
    command_topic: "home/bedroom/switch5"
    payload_on: "ON"
    payload_off: "OFF"
    state_on: "ON"
    state_off: "OFF"

  - platform: mqtt
    name: "Bedroom Switch6"
    state_topic: "home/bedroom/switch6/status"
    command_topic: "home/bedroom/switch6"
    payload_on: "ON"
    payload_off: "OFF"
    state_on: "ON"
    state_off: "OFF"

  - platform: mqtt
    name: "Bedroom Switch7"
    state_topic: "home/bedroom/switch7/status"
    command_topic: "home/bedroom/switch7"
    payload_on: "ON"
    payload_off: "OFF"
    state_on: "ON"
    state_off: "OFF"

  - platform: mqtt
    name: "Bedroom Switch8"
    state_topic: "home/bedroom/switch8/status"
    command_topic: "home/bedroom/switch8"
    payload_on: "ON"
    payload_off: "OFF"
    state_on: "ON"
    state_off: "OFF"

